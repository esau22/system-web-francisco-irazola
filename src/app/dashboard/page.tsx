import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Menu | Dashboard",
  description: "Menu Dashboard | Dashboard",
};

const MenuDashboard = () => {
  return <div>hola</div>;
};

export default MenuDashboard;
